import { Text, View } from 'react-native'

import { TextStyles } from '@/styles'

export default function ChartsScreen(): React.JSX.Element {
  return (
    <View>
      <Text style={TextStyles.base}>Charts</Text>
    </View>
  )
}
